/* Separator Component - Base Styles */

.separator {
    background-color: var(--border);
    flex-shrink: 0;
}

/* Horizontal */
.separator--horizontal {
    width: 100%;
    height: 1px;
}

/* Vertical */
.separator--vertical {
    width: 1px;
    height: 100%;
}
